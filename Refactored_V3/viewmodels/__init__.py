"""
viewmodels/ — MVVM ViewModel layer.

ViewModels expose Observable properties and Commands to the View layer.
They hold presentation state and delegate all business logic to the
core/ and services/ layers.  They must never import tkinter directly.
"""
